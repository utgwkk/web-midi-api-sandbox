import React from 'react';

class DJControllerSelector extends React.Component {
  render() {
    return (
      <select>
        <option value="not-selected">Select DJ controller</option>
      </select>
    );
  }
}

export default DJControllerSelector;
